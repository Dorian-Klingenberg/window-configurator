# Journal — May 21, 2026

## What We Did

Reviewed the project bootstrap process and corrected documentation drift from the Phase 4 pricing work.

The important bootstrap clarification is now explicit in `AGENTS.md`: the outer `renonerd/` folder is only the workspace container. The Git repository, solution, roadmap, ADRs, and normal command root are all inside `WindowConfigurator/`.

## Documentation Cleanup

Updated:

- `implementation-roadmap.md` — marked Phase 4 complete and Phase 5 active
- `README.md` — replaced stale in-memory/catalog-placeholder language with the current EF Core, server catalog, and server pricing state
- `window-domain-knowledge.md` — updated built/not-built and roadmap summary sections
- `adr/0005-completion-webhook-payload-contract.md` — updated pricing field language now that Phase 4 is complete
- `WindowConfigurator.Data/Webhooks/QuoteCompletedPayload.cs` — updated stale comments about pricing being null until Phase 4

## Phase 5 Started

Started server-side validation at completion using TDD.

Red tests were added first in `OrderItemControllerTests` for:

- frame width below catalog minimum
- unsupported operational style
- unsupported frame color
- product line outside the tenant's allowed catalog

Those tests initially failed because completion accepted invalid payloads.

## What Was Built

Added `CompletionValidationService` in `WindowConfigurator.Data/Validation/`.

The completion path now validates before pricing and persistence:

- tenant allowed product-line policy
- frame width/height against catalog frame restrictions
- section width/height against selected style restrictions
- operational style availability
- frame color, brickmould style, pane configuration, grille pattern, and SDL pattern availability

Invalid completion requests now return `400 Bad Request` with a `validationErrors` payload and do not mark the item/session completed.

## Verification

- `dotnet test .\WindowConfigurator.Tests\WindowConfigurator.Tests.csproj --nologo --filter "FullyQualifiedName~OrderItemControllerTests"` -> 14 passing
- `dotnet test .\WindowConfigurator.sln --nologo` -> 146 passing

## Current State

Phase 5 is active and has its first working validation slice. Remaining Phase 5 work should continue with TDD and broaden validation around section compatibility, mixed product-line policy, completion error shape, and any catalog rules currently enforced only in the Knockout client.

## Pricing Grid Validation

Continued Phase 5 with another TDD slice focused on the known pricing parity gap past final breakpoints.

Added direct `CompletionValidationServiceTests` for cases that are still inside catalog/UI restrictions but exceed a pricing component's final breakpoint:

- pane configuration overflow
- brickmould pricing-grid overflow

Those tests failed first, then `CompletionValidationService` was wired to the loaded `PriceInfoRoot` and now checks selected style, grille, SDL, pane configuration, and brickmould dimensions against their pricing breakpoint grids before completion pricing runs.

The pricing comparison tests still intentionally expose raw pricing-service parity gaps past final breakpoints, but completion validation now rejects those unsupported submissions before authoritative pricing is computed.

### Verification

- `dotnet test .\WindowConfigurator.Tests\WindowConfigurator.Tests.csproj --nologo --filter "FullyQualifiedName~CompletionValidationServiceTests|FullyQualifiedName~OrderItemControllerTests"` -> 16 passing
- `dotnet test .\WindowConfigurator.Tests\WindowConfigurator.Tests.csproj --nologo --filter "FullyQualifiedName~PricingComparisonMatrixTests|FullyQualifiedName~PricingCrossValidationTests"` -> 4 passing
- `dotnet test .\WindowConfigurator.sln --nologo` -> 148 passing

## Submitted Payload Pricing Discrepancy

User found a live mismatch in the browser: estimated price `$447.75`, authoritative server price `$467.72`.

The exact submitted payload was recovered from the local SQLite configuration snapshot and added as a regression fixture:

- `WindowConfigurator.Tests/Pricing/fixtures/submitted-44775-payload.json`

The failing test was added first:

- `Complete_WithSubmittedTwoSectionPayload_MatchesLegacyClientEstimate`

Root cause: the legacy JavaScript pricing engine uses three different measurement pairs:

- frame color pricing uses `osmWidth` / `osmHeight`
- section, grille, SDL, and pane pricing use each section's `width` / `height`
- brickmould pricing uses the payload's top-level `width` / `height`

The C# completion path was using `frameWidth` / `frameHeight` for brickmould pricing. In brickmould sizing mode those differ from the top-level `width` / `height`, which produced the `$19.97` discrepancy.

Fix:

- `WindowPricingInput` now carries explicit brickmould pricing dimensions
- `OrderItemController.BuildPricingInput` maps those from top-level `width` / `height`, falling back to `frameWidth` / `frameHeight`
- `PricingService.BrickmouldPrice` uses the explicit brickmould pricing dimensions
- completion validation checks brickmould pricing-grid support against the same dimensions

Verification was run using an alternate test output folder because the user's local debug app had the normal debug DLLs locked:

- `dotnet test .\WindowConfigurator.sln --nologo -p:BaseOutputPath=<repo>\artifacts\testbin\` -> 149 passing

## Pricing Regression Suite Update

Extended the pricing regression coverage so the submitted-payload mismatch is caught in more than one place.

The generated JavaScript comparison matrix now carries explicit brickmould pricing dimensions (`bw` / `bh`) separately from frame dimensions (`fw` / `fh`). A dedicated two-section submitted-payload regression case was added for the `$447.75` scenario where top-level `width` / `height` are `46` x `46 1/16`, while `frameWidth` / `frameHeight` are `66 9/16` x `42 3/8`.

Also added direct service and controller regressions:

- `CalculatePrice_BrickmouldUsesExplicitBrickmouldPricingMeasurements`
- `SubmittedTwoSectionBrickmouldSizingPayload_MatchesJavaScript`
- `Complete_WithSubmittedTwoSectionPayload_PricesBrickmouldFromTopLevelSizing`

One fixture-generator mistake was caught and corrected during this update: the submitted payload's second section has a stale display description, but the actual measurement object is `22 3/16`. The generator now uses the actual measurement data, matching the legacy JavaScript recomputation of `$447.75`.

### Verification

- `dotnet test .\WindowConfigurator.Tests\WindowConfigurator.Tests.csproj --nologo --filter "PricingComparisonMatrixTests|PricingCrossValidationTests|PricingServiceTests|OrderItemControllerTests" -p:BaseOutputPath=<repo>\artifacts\testbin\` -> 41 passing
- `dotnet test .\WindowConfigurator.sln --nologo -p:BaseOutputPath=<repo>\artifacts\testbin\` -> 152 passing

## Structure Changes Recorded

To prevent path confusion and make today’s work discoverable for future agents, structure-level notes were finalized:

- Added `WindowConfigurator/AGENTS.md` to explicitly state that `WindowConfigurator/` is the git/solution root for all normal commands.
- Updated the outer workspace `AGENTS.md` with the same bootstrap guardrail so agents do not run from `renonerd/` by mistake.
- Added `WindowConfigurator.Data/Validation/` with completion-validation service/contracts.
- Added `WindowConfigurator.Tests/Services/CompletionValidationServiceTests.cs`.
- Added `WindowConfigurator.Tests/Pricing/fixtures/submitted-44775-payload.json`.

Documentation that now reflects these changes:

- `README.md`
- `implementation-roadmap.md`
- `window-domain-knowledge.md`
- `WindowConfigurator.Tests/Pricing/README.md`

## Pricing Grid Alignment To Catalog Maximums

Adjusted pricing data handling so supported catalog dimensions no longer fall past the final pricing breakpoint.

Added `PricingGridAligner` (`WindowConfigurator.Data/Pricing/PricingGridAligner.cs`) and wired it at startup in `Program.cs` immediately after loading `priceInfo.json`.

Behavior:

- For each product line, the aligner reads the corresponding `*ItemTemplate.json` in `AppData`.
- Style pricing grids are extended to each style's catalog `maxWidth`/`maxHeight`.
- Grille, SDL, and pane configuration grids are extended to the product line's maximum section dimensions across styles.
- Brickmould grids are extended to frame max dimensions plus maximum brickmould inset on both sides.
- New breakpoint values are projected by continuing the last local slope in the existing pricing table.

Updated tests:

- Added `PricingGridAlignerTests` to verify breakpoint coverage reaches catalog limits per product line.
- Updated `CompletionValidationServiceTests` so catalog-valid near-maximum dimensions no longer fail with pricing-grid overflow errors.

### Verification

- `dotnet test .\WindowConfigurator.Tests\WindowConfigurator.Tests.csproj --nologo --filter "CompletionValidationServiceTests|PricingGridAlignerTests" -p:BaseOutputPath=<repo>\artifacts\testbin\` -> 3 passing
- `dotnet test .\WindowConfigurator.sln --nologo -p:BaseOutputPath=<repo>\artifacts\testbin\` -> 153 passing
