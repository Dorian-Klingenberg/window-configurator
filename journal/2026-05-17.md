# Journal — May 17, 2026

## Part One: Speculative Ideation

---

## Where the Conversation Started

The session opened as deliberate speculation: *where does renonerd go after square windows?*

The honest answer is that the current WindowConfigurator was never meant to be the whole platform. Bay windows, bow windows, doors — those are obvious extensions. But I wanted to think about more ambitious territory. The question I was really asking was: **what other products in the renovation industry have the same configurability profile as windows?**

A window is a configured manufactured good. It has a fixed set of options organized along a documented decision tree, each option affects price in a structured way, and the output is a precise specification that gets handed to a manufacturer. That combination — structured options, pricing logic, precise output spec — is what makes a configurator worthwhile. If the product doesn't have that structure, you're just building a form.

The easy answers (tiles, paint, countertops) were ruled out quickly. They don't have complex configuration trees. The more interesting candidates:

- **Staircases** — complicated. Rise, run, tread depth, handrail profiles, baluster spacing, landing geometry. Structural code compliance varies by jurisdiction. Very high configuration complexity.
- **Custom millwork** (crown, casing, built-ins) — intricate but highly bespoke. Hard to parametrize well without becoming a CAD tool.
- **Flooring patterns** — unusual candidate, not a "configured product" in the traditional sense, but interesting computationally: you could design custom medallions and borders and compute exact linear footage, cut angles, and tile quantities. More of a design tool than a configurator.
- **Stained glass inserts** — this one kept pulling attention.

---

## Why Stained Glass

Stained glass stood out for a few reasons that converged in a useful way.

First, it's genuinely parametric. A stained glass design is geometry — line positions, color regions, border structure. If you store a design as relative coordinates (everything in 0.0–1.0 space), you can scale it to any physical dimension without redesigning it. That's the same mathematical structure as a window section configuration. It maps onto the platform's existing configurator model very naturally.

Second, there's an obvious demand story. Stained glass inserts were mainstream in Victorian and Craftsman residential architecture. They were common enough that every contractor of that era knew what to do with them. What happened wasn't that taste changed — it was that postwar tract housing moved the market to cheap aluminum and fiberglass, and the custom fabrication process was too friction-heavy and expensive to survive at residential scale. The product category exists. People still like it. The price point and friction killed it.

Third — and this is what made it interesting rather than just nostalgic — **the configurator removes exactly the friction that killed the market.** The primary cost in a one-off stained glass job has always been the design consultation: the back-and-forth, the sketches, the custom quoting, the liability of hand-cutting to a custom dimension someone else measured. A pattern library with scaling eliminates most of that. The artist's skilled time goes into fabrication, not design from scratch every time.

---

## The Manufacturing Problem

The first real obstacle: *how does a stained glass panel become a residential window component?*

The obvious answer — just put it in a window — runs into the dual-glaze problem. Standard US residential windows are dual-glazed IGUs (insulated glass units). There is no cavity to put anything in. A panel hung between two panes of a dual-glazed unit isn't thermally protected, and the IGU fabrication process can't accommodate it.

Triple-glaze changes the geometry. Three panes, two cavities. The stained glass panel lives in the middle cavity. Both surfaces are protected. The panel is sealed, and the exterior and interior of the installation are unambiguously clear glass. This is a real and established product category — it's just not common in the US residential market because triple-glaze itself has historically been uncommon here.

That left the question: **can you actually get triple-glaze for a residential window project, or is this theoretical?**

The answer is yes, through two distinct paths:

1. **Unglazed frames.** Window manufacturers will sell you a frame without a sealed unit. The glazing is a separate purchase. You order your frame, you order your triple-glazed stained glass IGU, and the dealer installs it. This is the cleanest path for new replacement window orders.

2. **Replacement sealed units.** This is the more interesting discovery: if a dealer previously sold a customer an AWW window — EnergySaver 2500, Apex, or Carriage — and the `ProductLineKey` is in the configurator session, the platform already knows the rebate depth and spacer geometry for that frame. A replacement triple-glazed unit can be specced to drop directly into the existing frame without replacing the window at all. The dealer doesn't need to resell the whole window. They just sell the upgrade.

That second path turns the window dealer into a **repeat-purchase channel**. The window they sold three years ago becomes the sales lead for today's stained glass insert. That's the kind of data leverage that makes the `ProductLineKey` field on `ConfiguredWindowItemEntity` more than a catalog shorthand.

---

## The Sealing Problem and the Metal Border

Even with triple-glaze resolved, there's a fabrication problem: **the sealing surface.**

A standard IGU is fabricated by sandwiching two panes of glass with a spacer bar around the perimeter, applying sealant, and crimping the assembly. The sealing surface has to be continuous and flat. Traditional leaded glass doesn't provide that — the solder joints, lead lines, and edge conditions are irregular. You can't gasket directly against them.

The solution is a **solid metal border** — an aluminum or zinc channel that wraps the perimeter of the leaded glass field. The field terminates inside the channel and never touches the outer edge. The channel provides a flat, continuous surface that the IGU fabricator can seal against.

This separates the two fabrication concerns cleanly:
- The **stained glass studio** handles the artistic work: cutting, leading, soldering, assembling the field. They deliver a panel with a clean metal border already in place.
- The **IGU fabricator** handles the industrial work: sandwiching the bordered panel between two clear panes, sealing, and finishing. They treat it as a standard triple-glaze job with an unusual middle layer.

The studio and the IGU fabricator don't need to be the same entity. That's actually better — it lets each party work within their specialty.

---

## The Demand Question

Honest uncertainty here. The supply-side friction explanation is compelling but unproven. Is there real latent demand, or did it disappear for good with the architectural styles that carried it?

A few indicators suggest the latent demand argument holds:

- Stained glass inserts used to be *very* popular — mainstream enough that they were standard in entire architectural periods, not a niche luxury.
- The decorative window film market exists and is large, which suggests people do want decorative treatments for existing windows. Films are a friction-reduced approximation of what stained glass actually delivers.
- Custom stained glass studios still operate, and they report sustained demand for restoration and new commissions. The market didn't die — it just got expensive and hard.

But none of that proves that a configurator-driven parametric product at a lower price point would create the volume needed to sustain a marketplace. It's a hypothesis, not a conclusion.

---

## The Marketplace Model

The logical distribution channel is **window dealers**, not direct-to-consumer.

The dealer is already in the home. They're already measuring. The frame measurement is already in the session. The daylight opening — the visible glass area that the stained glass panel needs to fill — is derivable from the frame dimensions already on record. The insert becomes a line item in the same quote session. No second visit, no second measurement appointment.

The stained glass studio becomes a **fulfillment partner tenant** — a new tenant type in the existing multi-tenant architecture. Same platform, same session model, different role and different webhook payload. The sales tenant (window dealer) owns the customer relationship and fires the order. The fulfillment tenant (studio) receives the fabrication spec webhook and delivers.

**Supply chain roles:**
- **Window dealer (sales tenant)** — customer relationship, measurements, quote session, final order trigger
- **Stained glass studio (fulfillment tenant)** — receives job spec with daylight opening, pattern ID, scale/tile configuration; fabricates and delivers the panel
- **IGU fabricator** — receives spec from studio; seals the final triple-glazed unit (may or may not be the same entity as the studio)

The multi-tenant architecture already supports this structure in concept. The tenant types, webhook payloads, and permission model would need to distinguish sales tenants from fulfillment tenants, but the underlying session model is the same.

---

## Core Idea

A configurator for stained glass inserts — decorative leaded glass panels, custom-dimensioned to fit any window opening — built on the same session/tenant/webhook architecture already designed for window configuration.

The pattern library stores designs as relative coordinate geometry (0.0–1.0 space). A scaling/tiling engine maps patterns to the actual daylight opening. The homeowner picks a pattern, their measurement is already on file, and the platform produces a precise cut list for the studio and a sealed-unit spec for the IGU fabricator. The design consultation problem disappears. The artist's skilled time goes into fabrication.

---

## Open Questions

- Is there latent demand, or did it go away for good? The supply-side friction explanation is plausible but unproven.
- What's the minimum viable pattern library? How many designs does a launch need?
- Who owns the metal border fabrication step — the studio or the IGU fabricator?
- Can single-unit IGU orders be made economically viable, or does this require batching across multiple jobs?
- How does the fulfillment tenant data model differ from the sales tenant data model? New tenant type, or a role/capability flag on the existing `TenantEntity`?
- Are there studios with enough volume to be credible first fulfillment partners?

---

## Relation To Current Roadmap

This is a post-Phase 8 concept. The existing roadmap (Phases 0–9) builds the authoritative backend for the window configurator. The stained glass insert product builds on top of that foundation — it needs real persistence, server-side pricing, webhook dispatch, and multi-tenancy to be fully working before any of this makes sense to build.

No changes to the current roadmap are implied. This is a future product extension, not a near-term feature. It was catalogued here so the idea doesn't disappear.

## Part Two: Phase 1 Implementation Work

The second half of the day was focused and productive. We completed all three Phase 1 deliverables from the roadmap: the C# domain models, the architectural decision records, and the completion payload contract. This is the work that prevents major rework in every phase that follows.

---

## Where We Started

Before today, the codebase had `OrderEntity` (customer name, address, hardcoded Salesforce IDs) and `OrderItemEntity` (literally just a Guid). Those were leftovers from the original NALP commerce integration. They implied order fulfillment semantics — inventory, payment, commitment — none of which belong in a pre-order configuration platform. Nobody had cleaned them up because the configurator UI worked around them and the real domain model hadn't been defined yet.

The right move was to define the real model first and let everything else follow from it.

---

## The Three Domain Entities

### Why TenantEntity first

Tenant had to come first because everything else belongs to a tenant. Without a tenant record, there's no place to hang API credentials, webhook callback URLs, product line access rules, or branding configuration. Building sessions or items without a tenant anchor would produce a model that's technically functional but architecturally rootless — you'd have to retrofit multi-tenancy later, which is expensive.

`TenantEntity` captures:
- `ApiKey` — how the CRM authenticates
- `WebhookCallbackUrl` — where completed quotes get POSTed
- `AllowedProductLineKeys` — which product lines the dealer is licensed for
- `MixedProductLinesAllowed` — a policy flag discussed in detail below
- `Branding` — logo and colors for white-label embedding

The branding is kept as a nested `TenantBrandingConfig` object rather than a JSON blob or separate table. Either of those would work fine when persistence comes in, and the object shape gives us strong typing now without committing to a storage strategy.

### Why QuoteSession next

`QuoteSessionEntity` replaces `OrderEntity`. The name change matters. An "order" implies something has been purchased. A "quote session" implies something is being explored and priced — which is what this system actually does. It's pre-order. The CRM handles post-submission.

The key design decisions in `QuoteSessionEntity`:

**`DefaultProductLineKey` is nullable and named "default," not "productLine."** This was deliberate. For CRM-initiated sessions, the CRM often knows the product line and passes it in at session creation. For website-initiated sessions with a tenant who allows only one product line, it gets set automatically. But for mixed-product-line quotes (see below), it's a hint, not a constraint. Naming it "default" makes that distinction explicit.

**`ExternalReferenceId`** carries the CRM's opportunity or record ID back out in the webhook payload. The CRM doesn't need to store our ID — it already has its own. This field lets the two systems correlate without either one needing to know the other's schema.

**`MagicLinkToken` and `MagicLinkExpiresAt`** exist only for website-initiated sessions. Their presence or absence implicitly identifies which flow created the session. A null magic link token = CRM-initiated. Non-null = prospect. This is simpler than a separate discriminator field.

**`ExpiresAt`** exists because sessions that get created and abandoned shouldn't live forever. A CRM that creates a session and never sends the contractor is a real scenario. Expiry gives us a clean state to transition to.

### Why ConfiguredWindowItem last

`ConfiguredWindowItemEntity` replaces `OrderItemEntity` (which was just a Guid). This is where the richest domain knowledge lives.

The most important structural decision was **storing the full configuration as a JSON blob (`ConfigurationJson`) alongside promoted scalar fields.** The alternative — fully normalizing every option (style, grille pattern, SDL settings, pane configuration, etc.) into relational columns — would be expensive to design, fragile to maintain as the configurator evolves, and unnecessary for the primary use cases. The CRM doesn't need to query "show me all windows with Ladder grille pattern." The server pricing engine and validation engine need to read the full configuration, which they can do from the JSON blob. What does need to be queryable is promoted: frame dimensions, rough opening dimensions, outside dimensions, product line, section count, price, status.

**The three measurement pairs** were a significant domain conversation today.

The system originally had variables called `osmWidth`/`osmHeight` in the JavaScript — "outside measure" — and I had initially described these as the manufacturing-critical dimension. That was corrected with important nuance:

1. **Frame measurement** — outside edge to outside edge of the inner frame including the jamb extension. This is the window unit size, what you order from the manufacturer.

2. **Rough opening** — the structural hole in the wall. The rule of thumb is frame + ½" on all sides, but that's an approximation. Before a purchase order is placed, a contractor does a **remeasure visit**: casing comes off, the actual opening is measured. The real rough opening can differ from the estimate. This means rough opening must be stored as a first-class field, not derived. The null fields (`RoughOpeningWidthDecimal`, `RoughOpeningHeightDecimal`) are meaningful state — null means the remeasure hasn't happened yet, which means the item is not order-ready.

3. **Outside measurement** — the dimension that must fit within the existing brick or vinyl surround. This is variable because it depends on whether the old window had brickmould, whether the new window has brickmould, and how the original installation was done. If the original install was brickmould-to-brick and the new window also has brickmould, the outside measurement is brickmould-to-brickmould. If you're fitting into a tight vinyl surround with no brickmould clearance, the outside measurement is the frame. These are genuinely different numbers and cannot be computed from the frame alone — they require the brickmould configuration as an input.

The workflow implication: rough opening null = quote in progress. Rough opening populated = contractor has remeasured, item is order-ready. This distinction will matter for the API validation layer in Phase 6.

---

## Mixed Product Lines

This came up as a nuanced domain question. The `MixedProductLinesAllowed` flag on `TenantEntity` was discussed in context of the difference between a quote and a purchase order.

At the **quote stage**, mixing product lines is entirely legitimate. A contractor is pricing a whole house renovation. Some windows might be Apex (mid-range), some might be EnergySaver (budget), depending on location, egress requirements, or customer preference. The quote is an estimate — flexibility makes sense.

At the **order stage**, mixing product lines is operationally impossible. Window manufacturers produce per product line. You cannot mix Apex and EnergySaver on one purchase order. A mixed-product-line quote, when converted to purchase, must become two separate orders.

This has a direct implication for the webhook payload: a completed quote with mixed product lines must group items by product line in the payload. The `orderGroups` structure in `QuoteCompletedPayload` was designed for exactly this. The CRM admin who receives it can create one purchase order per group. Most quotes will have one group — the structure costs nothing for the common case but handles the multi-product-line case correctly without a breaking change.

---

## Architecture Decision Records

We recognized today that significant architectural decisions had been made in the May 11 and May 13 sessions without being formally recorded. Journals are narrative and retrospective — they capture what happened, not what was decided and why. ADRs are structured for future reference: context, decision, alternatives rejected, consequences.

Five ADRs were written, four of them backfilled from the journals:

- **ADR 0001** — Unidirectional webhook model. We never call the CRM. The CRM expert on the tenant's side owns their integration. We fire a generic webhook; they interpret it. This keeps the platform CRM-agnostic and eliminates adapter maintenance forever.

- **ADR 0002** — Two-flow architecture. CRM-initiated (contractor with machine authentication) and website-initiated (prospect with magic link) share the same session model and webhook pattern. The flows differ in how they start, not in what they produce.

- **ADR 0003** — Session-centric model. "Order" was the wrong word for what this system does. The CRM owns the downstream sales lifecycle. We own pre-order configuration state. That boundary clarification drove the rename from `OrderEntity`/`OrderItemEntity` to `QuoteSession`/`ConfiguredWindowItem`.

- **ADR 0004** — Product line as session-level input. Product line is a project-level decision in real contractor workflows, usually provided by the CRM context. Treating it as a casual per-window UI choice was a carryover from the original commerce integration that didn't reflect reality.

- **ADR 0005** — Completion webhook payload contract. The most consequential for downstream consumers. Once a CRM admin builds against this payload, breaking it is expensive. The structure was designed to be stable: guaranteed fields are guaranteed, nullable fields are documented as nullable with explicit business reasons, and the order-group structure handles mixed product lines without requiring a v2 of the payload.

The lesson going forward: write ADRs the same day decisions get made, not backfilled from journals weeks later. The journals are still valuable — they're the narrative record — but they don't replace structured decision documentation.

---

## Test Scaffold

Testing came up in the context of "we should get some testing into the data layer starting now." The reasoning was sound: the configurator JS isn't unit-testable in its current form, and trying to retrofit a test project after the repository layer is built is harder than building it before. The scaffolding cost today is low. The payoff comes in Phase 2 (repository layer — can we save and load a session correctly?) and Phase 4 (pricing — does the server compute the right price for a given configuration?).

The 18 tests written today are deliberately thin — the entities are POCOs with no business logic. What the tests do cover:
- Default values are correct (Draft status, null pricing, null rough opening, null magic link)
- The two session flow shapes (CRM-initiated and website-initiated) produce the expected field presence/absence
- All three measurement pairs can be independently stored
- Items can be linked to sessions

More importantly, the test project is in the solution, the reference to `WindowConfigurator.Data` is wired up, and the CI baseline is established. Every future phase has somewhere to put its tests immediately.

---

## Domain Knowledge Capture

The `window-domain-knowledge.md` file was created to serve a specific purpose: providing a comprehensive reference document for AI-assisted development work in future sessions. It captures everything discussed today about the industry — the three measurements and their lifecycle, the quote-vs-order distinction, the remeasure workflow, the contractor vs. prospect distinction, product lines, operational styles, grille patterns, the fractional measurement system, the pricing model, and the platform architecture.

The intent is that future sessions can point to this document and not have to re-derive domain context from scratch.

---

## What Phase 1 Accomplished

At the start of today, the codebase had two stub entities, no documented architectural decisions, and no tests.

At the end of today:
- Three real domain entities with correct semantics, correct field shapes, and documented reasoning
- Five ADRs covering the foundational architectural decisions
- A completion webhook payload contract that downstream consumers can build against
- A test project with 18 passing tests and a clear path for growth
- A domain knowledge document for future reference
- Everything committed cleanly to main

Phase 2 — real persistence — can now start from a stable foundation. The entities are defined. The session model is correct. The repository layer has something real to persist.
